const {john, peter} = require('./4-names')
const sayHi = require('./5-utils')
const data = require('./6-alternate-method')
require('./7-mind-grenade') //the code will run even though we didn't assign it to a variable and we didn't invoke that variable

// console.log(data)



//calling out the function
// sayHi('susan')
// sayHi(john)
// sayHi(peter)